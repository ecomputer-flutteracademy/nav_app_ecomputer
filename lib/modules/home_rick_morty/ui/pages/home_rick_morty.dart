import 'package:flutter/material.dart';

class HomeRickMorty extends StatelessWidget {
  const HomeRickMorty({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text("Home RICK AND MORTY"),
      ),
    );
  }
}
